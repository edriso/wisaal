// Moved to the shared kernel. Re-exported here so existing imports of './send'
// keep working unchanged.
export { sendMessage } from 'telegram-bot-kit';
export type { SendResult } from 'telegram-bot-kit';
